/**
 * L'observabilité transverse : identifiant de corrélation et journaux corrélés.
 *
 * <p>Ici plutôt que dans un module fonctionnel : un identifiant de corrélation ne vaut que s'il
 * est le même partout, et rien n'est plus inutile qu'une corrélation qui s'arrête à la frontière
 * d'un domaine.
 */
package com.runtrack.platform.observability;
